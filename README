Build:
mvn install ausfuehren
Vorbedingungen: Maven + JDK installiert

Run:
Das Binary liegt in target/encoder.jar
Start mittels java -jar encoder.jar

Bemerkungen:
Die Ranges sind Annahmen, diese lassen sich aufgrund der vorliegenden Spezifikation eigentlich so nicht
herleiten. Streng genommen dürfen eigentlich nur genau die Character gemappt werden,
welche in den Input/Output Files spezifiziert sind.